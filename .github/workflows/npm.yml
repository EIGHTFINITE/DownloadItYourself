name: "npm"

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 0 * * *'

jobs:
  npm_linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.3.0
      with:
        path: EIGHTFINITE-build
    - name: npm
      shell: bash
      run: |
        cd EIGHTFINITE-build
        bash --noprofile --norc -e -o pipefail docs/tools/git-config.sh
        git fetch --depth 1 origin +refs/tags/artifacts:refs/tags/artifacts --no-tags
        git restore --source=artifacts -- bin/
        export node_version=$(cat package.json | python -c "import sys, json; print(json.load(sys.stdin)['engines']['node'])")
        export npm_version=$(curl -sS 'https://registry.npmjs.org/npm' | python -c "import sys, json; print(json.load(sys.stdin)['dist-tags']['latest-6'])")
        wget -nv -O "npm-$npm_version.tgz" "https://registry.npmjs.org/npm/-/npm-$npm_version.tgz"
        mkdir -p "bin/all/all/npm/npm-$npm_version/npm"
        tar -xzf "npm-$npm_version.tgz" --strip-components=1 -C "bin/all/all/npm/npm-$npm_version/npm"
        rm "npm-$npm_version.tgz"
        echo -n "$node_version" > node_version.txt
        echo -n "$npm_version" > npm_version.txt
        bash --noprofile --norc -e -o pipefail docs/tools/actions-npm.sh
        bin/linux/x64/node/node-v$node_version-linux-x64/bin/node node_modules/npm/bin/npm-cli.js ls --no-unicode --loglevel=silent || true
        rm -rf .npm/
        cd ..
        echo '{
          "private": "true"
        }' > package.json
        cp EIGHTFINITE-build/.npmrc .npmrc
        EIGHTFINITE-build/bin/linux/x64/node/node-v$node_version-linux-x64/bin/node EIGHTFINITE-build/node_modules/npm-6/bin/npm-cli.js install ./EIGHTFINITE-build
        rm -rf .npm/
  npm_windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3.3.0
      with:
        path: EIGHTFINITE-build
    - name: npm
      shell: bash
      run: |
        cd EIGHTFINITE-build
        docs/tools/git-config.bat
        git fetch --depth 1 origin +refs/tags/artifacts:refs/tags/artifacts --no-tags
        git restore --source=artifacts -- bin/ node_modules/ package-lock.json
        export node_version=$(cat package.json | python -c "import sys, json; print(json.load(sys.stdin)['engines']['node'])")
        bin/windows/x64/node/node-v$node_version-win-x64/node.exe node_modules/npm/bin/npm-cli.js ls --no-unicode --loglevel=silent || true
        rm -rf .npm/
        cd ..
        echo '{
          "private": "true"
        }' > package.json
        cp EIGHTFINITE-build/.npmrc .npmrc
        EIGHTFINITE-build/bin/windows/x64/node/node-v$node_version-win-x64/node.exe EIGHTFINITE-build/node_modules/npm-6/bin/npm-cli.js install .\\EIGHTFINITE-build
        rm -rf .npm/
  audit_linux:
    runs-on: ubuntu-latest
    needs: [npm_linux, npm_windows]
    if: ${{ needs.npm_linux.result == 'success' && needs.npm_windows.result == 'success' }}
    steps:
    - uses: actions/checkout@v3.3.0
    - name: audit
      shell: bash
      run: |
        bash --noprofile --norc -e -o pipefail docs/tools/git-config.sh
        git fetch --depth 1 origin +refs/tags/artifacts:refs/tags/artifacts --no-tags
        git restore --source=artifacts -- bin/
        export node_version=$(cat package.json | python -c "import sys, json; print(json.load(sys.stdin)['engines']['node'])")
        export npm_version=$(curl -sS 'https://registry.npmjs.org/npm' | python -c "import sys, json; print(json.load(sys.stdin)['dist-tags']['latest-6'])")
        wget -nv -O "npm-$npm_version.tgz" "https://registry.npmjs.org/npm/-/npm-$npm_version.tgz"
        mkdir -p "bin/all/all/npm/npm-$npm_version/npm"
        tar -xzf "npm-$npm_version.tgz" --strip-components=1 -C "bin/all/all/npm/npm-$npm_version/npm"
        rm "npm-$npm_version.tgz"
        echo -n "$node_version" > node_version.txt
        echo -n "$npm_version" > npm_version.txt
        bash --noprofile --norc -e -o pipefail docs/tools/actions-npm.sh
        mkdir -p "bin/linux/x64/node/node-v$node_version-linux-x64/lib/node_modules"
        cp -r "node_modules/npm" "bin/linux/x64/node/node-v$node_version-linux-x64/lib/node_modules/npm"
        export old_PATH="$PATH"
        export PATH="$(pwd)/bin/linux/x64/node/node-v$node_version-linux-x64/bin:$PATH"
        bin/linux/x64/node/node-v$node_version-linux-x64/bin/node node_modules/better-npm-audit/index.js audit
        export PATH="$old_PATH"
        rm -rf .npm/
        rm -r "bin/linux/x64/node/node-v$node_version-linux-x64/lib"
  audit_windows:
    runs-on: windows-latest
    needs: [npm_linux, npm_windows]
    if: ${{ needs.npm_linux.result == 'success' && needs.npm_windows.result == 'success' }}
    steps:
    - uses: actions/checkout@v3.3.0
    - name: audit
      shell: bash
      run: |
        docs/tools/git-config.bat
        git fetch --depth 1 origin +refs/tags/artifacts:refs/tags/artifacts --no-tags
        git restore --source=artifacts -- bin/ node_modules/ package-lock.json
        export node_version=$(cat package.json | python -c "import sys, json; print(json.load(sys.stdin)['engines']['node'])")
        mkdir -p "bin/windows/x64/node/node-v$node_version-win-x64/node_modules"
        cp -r "node_modules/npm" "bin/windows/x64/node/node-v$node_version-win-x64/node_modules/npm"
        export old_PATH="$PATH"
        export PATH="$(pwd)/bin/windows/x64/node/node-v$node_version-win-x64:$PATH"
        bin/windows/x64/node/node-v$node_version-win-x64/node.exe node_modules/better-npm-audit/index.js audit
        export PATH="$old_PATH"
        rm -rf .npm/
        rm -r "bin/windows/x64/node/node-v$node_version-win-x64"
