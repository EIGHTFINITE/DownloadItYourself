name: "Lock"

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
    - uses: dessant/lock-threads@v3
      with:
        add-issue-labels: 'locked'
        issue-lock-reason: ''
        add-pr-labels: 'locked'
        pr-lock-reason: ''
